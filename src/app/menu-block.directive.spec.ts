import { MenuBlockDirective } from './menu-block.directive';

describe('MenuBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuBlockDirective();
    expect(directive).toBeTruthy();
  });
});
